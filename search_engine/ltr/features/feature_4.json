{
    "query": {
        "match_explorer": {
            "type": "max_raw_df",
            "query": {
                "match": {
                    "all": "{{consulta}}"
                }
            }
        }
    }
}